apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wga.fullname" . }}-test-connection"
  labels:
    {{- include "wga.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wga.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
