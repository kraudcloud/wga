apiVersion: v1
kind: Service
metadata:
  name: {{ include "wga.fullname" . }}
  labels:
    {{- include "wga.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wga.selectorLabels" . | nindent 4 }}
